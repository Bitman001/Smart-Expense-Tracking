{
  "name": "smart-expense",
  "version": "1.0.0",
  "private": true,
  "description": "智能记账 App - 说一句话或拍张照，自动帮你记账",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "server:dev": "cd apps/server && npm run dev",
    "server:start": "cd apps/server && npm start",
    "mobile:start": "cd apps/mobile && npx expo start",
    "db:migrate": "cd apps/server && npx prisma migrate dev",
    "db:seed": "cd apps/server && npx prisma db seed",
    "db:generate": "cd apps/server && npx prisma generate"
  }
}
