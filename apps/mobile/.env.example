# 复制为 .env.development(本地) 或 .env.production(打包时) 再填写。
# Expo 只会打包以 EXPO_PUBLIC_ 开头的变量到客户端 bundle。

# 后端 API 基础地址(末尾不要带斜杠)
# 本地开发: http://localhost:3000/api
# Railway/生产: https://your-railway-domain.up.railway.app/api
EXPO_PUBLIC_API_URL=https://your-railway-domain.up.railway.app/api
